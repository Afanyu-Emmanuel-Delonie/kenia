services:
  - type: web
    name: kenia-backend
    runtime: docker
    dockerfilePath: backend/kenia/Dockerfile
    dockerContext: backend/kenia
    healthCheckPath: /api/v1/health
    autoDeployTrigger: commit
